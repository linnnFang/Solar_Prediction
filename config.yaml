# Edit your paths here — this is the only file you need to touch.
gefcom_dir: /Users/lin_/Solar_Prediction/data/raw/GEFCOM
processed_dir: /Users/lin_/Solar_Prediction/data/process

# Site info (local time = UTC + tz_offset). Derived from data: SSRD peaks at 02:00 UTC.
site:
  tz_offset: 10
